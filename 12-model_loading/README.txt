This project teaches how to load a 3D model using assimp. It allows us to use 3D pre-existing models and avoid to take care about creating vertices and normals to
take the next steps.

Moreover, i used uniform buffers to send the material properties for each mesh to the opengl pipeline.